# postgres user in db is like root user in linux
# psql -U postgres


psql -U neo -d neos_db

# - `-U neo`: Connect as the user `neo`.
    
# - `-d neos_db`: Connect to the **d**atabase named `neos_db`.
    

